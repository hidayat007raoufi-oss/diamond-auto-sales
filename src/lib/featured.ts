/**
 * Homepage performance hero — swappable slot.
 *
 * Set FEATURED_PERFORMANCE_ID to the id of a REAL vehicle in src/lib/vehicles.ts
 * to feature it as the marquee performance car. While it's null, the hero shows
 * a clearly-labeled placeholder (no fake inventory claims) — drop in a real car
 * anytime by setting the id here.
 *
 * Example: export const FEATURED_PERFORMANCE_ID = "cadillac-cts-2015-36-rwd";
 */
export const FEATURED_PERFORMANCE_ID: string | null = null;
